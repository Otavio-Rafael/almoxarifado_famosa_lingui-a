@using X.PagedList
@using X.PagedList.Mvc.Core
@model X.PagedList.IPagedList<Moxarifi.Models.MovimentacaoEstoque>

@{
    ViewData["Title"] = "Movimentações";
}

<h1>Movimentações de Estoque</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Nova Movimentação</a>
</p>

<table class="table table-hover">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.FirstOrDefault().Quantidade)</th>
            <th>Produto</th>
            <th>@Html.DisplayNameFor(model => model.FirstOrDefault().Data)</th>
            <th>@Html.DisplayNameFor(model => model.FirstOrDefault().TipoMovi)</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Quantidade)</td>
                <td>@Html.DisplayFor(modelItem => item.Produto.Nome)</td>
                <td>@Html.DisplayFor(modelItem => item.Data)</td>
                <td>@Html.DisplayFor(modelItem => item.TipoMovi)</td>
                <td>
                   
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info">Detalhes</a>
                   
                </td>
            </tr>
        }
    </tbody>
</table>

<div class="d-flex justify-content-between align-items-center mt-3">
    <small class="text-muted">
        Página @Model.PageNumber de @Model.PageCount — @Model.TotalItemCount movimentação(ões) no total
    </small>
    @Html.PagedListPager(Model, pagina => Url.Action("Index", new { pagina }),
        new PagedListRenderOptions
        {
            LiElementClasses = new[] { "page-item" },
            PageClasses = new[] { "page-link" },
            UlElementClasses = new[] { "pagination" }
        })
</div>